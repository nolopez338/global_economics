/* Pre-transition stages are advanced by the presentation lifecycle. This optional
   marker keeps staged-transition assets explicit and provides an extension hook. */
window.MindMap?.register("pre-transitions", () => {});
