/* Criteria dialogs are initialized by the presentation lifecycle. This optional
   marker keeps criteria assets explicit and provides a stable extension hook. */
window.MindMap?.register("criteria", () => {});
