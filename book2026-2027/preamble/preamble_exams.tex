\makeatletter
\def\input@path{{./}{../}{../../}{../../../}{preamble/}{../preamble/}{../../preamble/}{../../../preamble/}}
\makeatother	
\input{preamble.tex}

\setlength{\parindent}{0pt}
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}

% ===================================================================
\setlist[itemize]{topsep=2pt,itemsep=1pt,parsep=0pt,partopsep=0pt}
\setlist[enumerate]{topsep=2pt,itemsep=0.8pt,parsep=0pt,partopsep=0pt}

% Alphabetical enumeration: a), b), c), ...
\newlist{alphenum}{enumerate}{1}
\setlist[alphenum]{
	label=\textbf{\alph*)},
	topsep=2pt,
	itemsep=0.8pt,
	parsep=0pt,
	partopsep=0pt
}


% ===================================================================
% Column types
\newlength{\YColumnWidth}

% Set Y as a fraction of \textwidth.
% Z automatically uses the remaining fraction.
\newcommand{\setYColumnWidth}[1]{%
	\setlength{\YColumnWidth}{#1\textwidth}%
}

% Default: Y = 30%, Z = 70%
\setYColumnWidth{0.30}

% Column types
\newcolumntype{Y}{%
	>{\raggedright\arraybackslash}%
	m{\dimexpr\YColumnWidth-2\tabcolsep-2\arrayrulewidth\relax}%
}

\newcolumntype{Z}{%
	>{\raggedright\arraybackslash}%
	m{\dimexpr\textwidth-\YColumnWidth-2\tabcolsep-2\arrayrulewidth\relax}%
}

\newcolumntype{C}[1]{%
	>{\centering\arraybackslash}m{#1}%
}


% Gray subsection header box
\newcommand{\SubsectionBox}[1]{%
	\vspace{0.25cm}
	\noindent\colorbox{gray!30}{%
		\parbox{\linewidth}{\textbf{#1}}%
	}\par\vspace{0.0cm}%
}

% Centered multi-line cell helper
\newcommand{\CellCenter}[1]{%
	\parbox{\linewidth}{\centering #1}%
}

% Centered multi-line cell helper
\newcommand{\score}[1]{%
	\hfill \textbf{[#1]}
}
