\makeatletter
\def\input@path{{./}{../}{../../}{../../../}{../../../../}{preamble/}{../preamble/}{../../preamble/}{../../../preamble/}}
\makeatother

\input{preamble_exams.tex}
\input{graphs/uniform_linear_probability_distributions.tex}
\input{graphs/probability_distribution_graphs.tex}
\input{graphs/normal_distribution_graphs.tex}

\title{Grade 11 -- Term 1 \\ C1 Practice Activity 1 with Solutions}
\author{}
\date{}

\begin{document}
	\maketitle

	\setcounter{tocdepth}{1}
	\setcounter{secnumdepth}{2}
	\phantomsection
	\label{toc}
	\tableofcontents

	\section*{Evaluated criterion}
	\begin{itemize}
		\item C1: Characterises continuous random variables by distinguishing them from discrete variables and interpreting their probability density function, cumulative distribution function, and support in economic contexts.
	\end{itemize}


	\section{Introduction}
	\label{sec:introduction}

	A \textbf{random variable} assigns a numerical value to the outcome of a random process. A discrete random variable takes separate, countable values (often counts such as $0,1,2,\ldots$). A continuous random variable can take any real value in an interval, such as a waiting time or price. Its \textbf{support} is the set of possible values.

	For a discrete variable, the probability mass function (PMF) $p_X(x)=P(X=x)$ gives probability at each possible value, and the masses sum to $1$. For a continuous variable, the probability density function (PDF) $f_X(x)$ is a height, not a point probability: $P(X=x)=0$, while interval probability is area, $P(r\le X\le s)=\int_r^s f_X(x)\,dx$. A valid density is nonnegative and has total area $1$. In both cases the cumulative distribution function (CDF)
	\[
		F_X(x)=P(X\le x)
	\]
	records the probability accumulated up to $x$; it is a sum of PMF values or an integral of the PDF.

	\subsection*{Bernoulli distribution (discrete)}
	A Bernoulli variable represents \emph{one} trial with two outcomes, coded success ($1$) and failure ($0$). Clues are ``one selected account,'' ``yes/no,'' or ``paid/defaulted.'' Its support is $\{0,1\}$ and its parameter is the success probability $0\le p\le1$:
	\[
		P(X=x)=p^x(1-p)^{1-x},\quad x\in\{0,1\},
		\qquad
		F_X(x)=\begin{cases}0,&x<0,\\1-p,&0\le x<1,\\1,&x\ge1.\end{cases}
	\]
	The PMF assigns the two outcome probabilities; the CDF gives the probability of a coded outcome no greater than $x$. For example, $X=1$ may mean that one randomly selected invoice is overdue.

	\subsection*{Binomial distribution (discrete)}
	A binomial variable counts successes in a \emph{fixed} number $n$ of independent Bernoulli trials having the same success probability $p$. Its recognition clues are a stated sample size, binary outcomes, independence, and constant $p$. Its support is $\{0,1,\ldots,n\}$ and its parameters are $n$ and $p$:
	\[
		P(X=x)=\binom{n}{x}p^x(1-p)^{n-x},\quad x=0,\ldots,n,
		\qquad F_X(x)=\sum_{k=0}^{\lfloor x\rfloor}\binom{n}{k}p^k(1-p)^{n-k},
	\]
	where the CDF is $0$ below $0$ and $1$ at or above $n$. The PMF gives the probability of exactly $x$ successes; the CDF gives that of at most $x$. An example is the number of renewed subscriptions among $n$ independently contacted customers.

	\subsection*{Geometric distribution (discrete)}
	Here $X$ counts the number of independent Bernoulli trials \emph{required to obtain the first success}; thus this activity uses support $\{1,2,3,\ldots\}$. Look for repeated independent attempts, constant success probability $p$, and stopping at the first success:
	\[
		P(X=x)=(1-p)^{x-1}p,\quad x=1,2,\ldots,
		\qquad F_X(x)=\begin{cases}0,&x<1,\\1-(1-p)^{\lfloor x\rfloor},&x\ge1.\end{cases}
	\]
	The PMF describes $x-1$ failures followed by a success; the CDF describes success occurring by trial $x$. For example, $X$ can be the number of sales pitches until the first order.

	\subsection*{Poisson distribution (discrete)}
	A Poisson variable counts events in a fixed time, distance, area, or other interval when events occur separately at an approximately constant average rate $\lambda>0$. Clues are an event count, a fixed interval, and a stable rate. Its support is $\{0,1,2,\ldots\}$:
	\[
		P(X=x)=e^{-\lambda}\frac{\lambda^x}{x!},\quad x=0,1,2,\ldots,
		\qquad F_X(x)=e^{-\lambda}\sum_{k=0}^{\lfloor x\rfloor}\frac{\lambda^k}{k!}.
	\]
	The PMF gives the chance of exactly $x$ events and the CDF the chance of no more than $x$. A call centre might use it for the number of incoming calls in a fixed five-minute period.

	\subsection*{Uniform distribution (continuous)}
	A uniform variable treats all values in a bounded interval $[a,b]$ as having equal density. A stated minimum and maximum plus ``equally plausible anywhere'' are decisive clues. Its support is $[a,b]$, with parameters $a<b$:
	\[
		f_X(x)=\begin{cases}\dfrac1{b-a},&a\le x\le b,\\0,&\text{otherwise},\end{cases}
		\qquad
		F_X(x)=\begin{cases}0,&x<a,\\\dfrac{x-a}{b-a},&a\le x\le b,\\1,&x>b.\end{cases}
	\]
	The constant PDF makes equal-length subintervals equally probable; the CDF is the fraction of the support lying at or below $x$. A courier arrival time known only to be equally plausible from 2:00 to 2:30 p.m. is an example.

	\subsection*{Triangular distribution (continuous)}
	A triangular variable has minimum $a$, maximum $b$, and mode (most likely value) $c$, where $a<c<b$. Density rises linearly toward $c$ and falls linearly afterward---the key distinction from one straight linear density across the whole support. Its parameters are $a,c,b$ and support is $[a,b]$:
	\[
	 f_X(x)=\begin{cases}
	 0,&x<a,\\
	 \dfrac{2(x-a)}{(b-a)(c-a)},&a\le x\le c,\\
	 \dfrac{2(b-x)}{(b-a)(b-c)},&c<x\le b,\\
	 0,&x>b,
	 \end{cases}
	\]
	\[
	 F_X(x)=\begin{cases}
	 0,&x<a,\\
	 \dfrac{(x-a)^2}{(b-a)(c-a)},&a\le x\le c,\\
	 1-\dfrac{(b-x)^2}{(b-a)(b-c)},&c<x\le b,\\
	 1,&x>b.
	 \end{cases}
	\]
	The PDF expresses increasing then decreasing plausibility; its accumulated area is the CDF. A project cost with an expert minimum, maximum, and most likely estimate is a common business example.

	\subsection*{Linear distribution (continuous)}
	A linear distribution has a PDF $f_X(x)=mx+d$ throughout a bounded support $[a,b]$, and $0$ outside it. The constants must satisfy $mx+d\ge0$ on $[a,b]$ and
	\[
		\int_a^b(mx+d)\,dx=\frac{m}{2}(b^2-a^2)+d(b-a)=1.
	\]
	Its support is $[a,b]$ and its parameters may be written $a,b,m,d$ subject to these restrictions. The CDF is
	\[
	 F_X(x)=\begin{cases}0,&x<a,\\\dfrac{m}{2}(x^2-a^2)+d(x-a),&a\le x\le b,\\1,&x>b.\end{cases}
	\]
	Clues specify one density line increasing or decreasing across the entire interval, without an interior mode. The PDF gives relative concentration and the CDF its accumulated area. For instance, longer checkout times from 0 to 8 minutes may have density $f(t)=t/32$, increasing linearly over the whole interval.

	\subsection*{Normal distribution (continuous)}
	A normal variable models approximately symmetric, bell-shaped observations clustered around a centre, with values farther away increasingly uncommon. Its support is all real numbers and its parameters are mean $\mu$ and standard deviation $\sigma>0$:
	\[
		f_X(x)=\frac{1}{\sigma\sqrt{2\pi}}\exp\!\left[-\frac{(x-\mu)^2}{2\sigma^2}\right],
		\qquad F_X(x)=\Phi\!\left(\frac{x-\mu}{\sigma}\right).
	\]
	Here $\Phi$ is the standard normal CDF (it is normally read from technology or a table). The PDF describes the bell's relative concentration and the CDF gives area to the left. A large firm's many employee commute times may be approximately normal when they are symmetric around a mean.

	\secInicio[sec:introduction]

	\section{Discrete Probabilities}
	\label{sec:discrete-probabilities}

	\SectionProblemsTableOfContents{
		\SectionProblemLink{sec:single-loan-review}{A Single Loan Review} \\
		\SectionProblemLink{sec:warranty-status}{Warranty Status at Inspection} \\
		\SectionProblemLink{sec:client-renewals}{Renewals in a Client List} \\
		\SectionProblemLink{sec:bottle-quality-check}{Quality Check for Bottles} \\
		\SectionProblemLink{sec:calls-before-appointment}{Calls Before an Appointment} \\
		\SectionProblemLink{sec:prototypes-until-approval}{Prototypes Until Approval} \\
		\SectionProblemLink{sec:lunch-orders}{Orders During Lunch} \\
		\SectionProblemLink{sec:road-claims}{Claims per Road Segment}
	}

	% ----------------------------------------------------------------
	\subsection{A Single Loan Review}
	\label{sec:single-loan-review}

	A credit union randomly selects one new business loan and records $X=1$ if the borrower makes the first repayment on time and $X=0$ otherwise. Only that one loan is observed, and the probability of an on-time payment is $0.92$.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:single-loan-review-c1}{1}
		{Characterise the repayment indicator $X$.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:single-loan-review-c1}

	The variable is \textbf{discrete} and has a \textbf{Bernoulli distribution}: there is one binary trial (on time or not on time) with a fixed success probability.

	\secInicio[sec:discrete-probabilities][sec:single-loan-review]

	% ----------------------------------------------------------------
	\subsection{Warranty Status at Inspection}
	\label{sec:warranty-status}

	An electronics retailer inspects one randomly selected returned tablet. Let $X$ be $1$ when its warranty claim is approved and $0$ when it is rejected; every claim must receive exactly one of these outcomes.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:warranty-status-c1}{1}
		{Characterise the claim-status variable $X$.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:warranty-status-c1}

	The variable is \textbf{discrete} and has a \textbf{Bernoulli distribution}: one claim produces exactly one of two coded outcomes.

	\secInicio[sec:discrete-probabilities][sec:warranty-status]

	% ----------------------------------------------------------------
	\subsection{Renewals in a Client List}
	\label{sec:client-renewals}

	An insurance agency contacts exactly $30$ clients whose annual policies expire this week and records the number who renew. Each client either renews or does not; decisions are treated as independent, with the same renewal probability for every client.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:client-renewals-c1}{1}
		{Characterise the number of renewals.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:client-renewals-c1}

	The variable is \textbf{discrete} and has a \textbf{binomial distribution}: it counts successes in a fixed $30$ independent binary trials with constant success probability.

	\secInicio[sec:discrete-probabilities][sec:client-renewals]

	% ----------------------------------------------------------------
	\subsection{Quality Check for Bottles}
	\label{sec:bottle-quality-check}

	A beverage company takes exactly $50$ bottles from a stable production run and counts how many pass a seal test. Each seal independently passes or fails, and the probability of passing is unchanged across the sample.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:bottle-quality-check-c1}{1}
		{Characterise the number of bottles that pass.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:bottle-quality-check-c1}

	The variable is \textbf{discrete} and has a \textbf{binomial distribution}: it is a success count from a fixed number of independent, equal-probability binary trials.

	\secInicio[sec:discrete-probabilities][sec:bottle-quality-check]

	% ----------------------------------------------------------------
	\subsection{Calls Before an Appointment}
	\label{sec:calls-before-appointment}

	A financial adviser calls prospective clients independently, with the same probability of booking an appointment on every call. Calling stops as soon as the first appointment is booked, and $X$ records the total number of calls made.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:calls-before-appointment-c1}{1}
		{Characterise the total number of calls $X$.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:calls-before-appointment-c1}

	The variable is \textbf{discrete} and has a \textbf{geometric distribution}: it counts independent trials required to reach the first success when the success probability remains constant.

	\secInicio[sec:discrete-probabilities][sec:calls-before-appointment]

	% ----------------------------------------------------------------
	\subsection{Prototypes Until Approval}
	\label{sec:prototypes-until-approval}

	A design studio produces prototypes one at a time. Each prototype is independently approved by the buyer with the same probability, and production ends at the first approval. The studio records how many prototypes it had to produce.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:prototypes-until-approval-c1}{1}
		{Characterise the number of prototypes produced.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:prototypes-until-approval-c1}

	The variable is \textbf{discrete} and has a \textbf{geometric distribution}: attempts continue until the first approval, under independent trials with constant success probability.

	\secInicio[sec:discrete-probabilities][sec:prototypes-until-approval]

	% ----------------------------------------------------------------
	\subsection{Orders During Lunch}
	\label{sec:lunch-orders}

	A cafe counts online orders received during each fixed $15$-minute lunch interval. Orders arrive separately, and historical data indicate that the average arrival rate stays stable during the period.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:lunch-orders-c1}{1}
		{Characterise the number of online orders.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:lunch-orders-c1}

	The variable is \textbf{discrete} and has a \textbf{Poisson distribution}: it counts events in a fixed time interval under an approximately constant average rate.

	\secInicio[sec:discrete-probabilities][sec:lunch-orders]

	% ----------------------------------------------------------------
	\subsection{Claims per Road Segment}
	\label{sec:road-claims}

	An insurer records the number of roadside-assistance claims originating along each fixed $10$-kilometre section of a long highway. Claims occur separately, and comparable sections have a stable average number of claims.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:road-claims-c1}{1}
		{Characterise the number of claims per section.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:road-claims-c1}

	The variable is \textbf{discrete} and has a \textbf{Poisson distribution}: it counts separate events over a fixed distance interval with a stable average rate.

	\secInicio[sec:discrete-probabilities][sec:road-claims]


	\section{Continuous Probabilities}
	\label{sec:continuous-probabilities}

	\SectionProblemsTableOfContents{
		\SectionProblemLink{sec:supplier-arrival-window}{Supplier Arrival Window} \\
		\SectionProblemLink{sec:discount-setting}{Promotional Discount Setting} \\
		\SectionProblemLink{sec:renovation-cost}{Renovation Cost Estimate} \\
		\SectionProblemLink{sec:seasonal-delivery}{Seasonal Delivery Duration} \\
		\SectionProblemLink{sec:checkout-duration}{Increasing Checkout Duration} \\
		\SectionProblemLink{sec:contract-value}{Short-Term Contract Value} \\
		\SectionProblemLink{sec:coffee-weights}{Packed Coffee Weights} \\
		\SectionProblemLink{sec:electricity-bills}{Monthly Household Electricity Bills}
	}

	% ----------------------------------------------------------------
	\subsection{Supplier Arrival Window}
	\label{sec:supplier-arrival-window}

	A restaurant is told that a produce truck will arrive at some time from 6:00 to 6:40 a.m. The dispatcher regards every instant in this bounded window as equally plausible. Let $T$ be the arrival time in minutes after 6:00.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:supplier-arrival-window-c1}{1}
		{Characterise the arrival time $T$.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:supplier-arrival-window-c1}

	The variable is \textbf{continuous} and has a \textbf{uniform distribution}: its bounded support is $[0,40]$ minutes and density is equal throughout.

	\secInicio[sec:continuous-probabilities][sec:supplier-arrival-window]

	% ----------------------------------------------------------------
	\subsection{Promotional Discount Setting}
	\label{sec:discount-setting}

	For a one-day online promotion, software selects a discount percentage anywhere from $5\%$ to $15\%$. Every real-valued percentage in that interval has the same density.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:discount-setting-c1}{1}
		{Characterise the selected discount percentage.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:discount-setting-c1}

	The variable is \textbf{continuous} and has a \textbf{uniform distribution}: values lie on a bounded interval and are equally plausible throughout it.

	\secInicio[sec:continuous-probabilities][sec:discount-setting]

	% ----------------------------------------------------------------
	\subsection{Renovation Cost Estimate}
	\label{sec:renovation-cost}

	A shop owner estimates a renovation cost between \$18,000 and \$30,000, with \$22,000 the most likely cost. The assessed density rises steadily from the minimum to \$22,000 and then falls steadily to the maximum.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:renovation-cost-c1}{1}
		{Characterise the renovation cost.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:renovation-cost-c1}

	The variable is \textbf{continuous} and has a \textbf{triangular distribution}: it has a minimum, maximum, and interior mode, with linear rise and fall in density.

	\secInicio[sec:continuous-probabilities][sec:renovation-cost]

	% ----------------------------------------------------------------
	\subsection{Seasonal Delivery Duration}
	\label{sec:seasonal-delivery}

	A wholesaler models delivery duration from $1$ to $7$ days, with $3$ days most likely. Density increases linearly from day $1$ to day $3$ and decreases linearly from day $3$ to day $7$.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:seasonal-delivery-c1}{1}
		{Characterise the delivery duration.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:seasonal-delivery-c1}

	The variable is \textbf{continuous} and has a \textbf{triangular distribution}: the bounded support, stated mode, and two linear sides identify it.

	\secInicio[sec:continuous-probabilities][sec:seasonal-delivery]

	% ----------------------------------------------------------------
	\subsection{Increasing Checkout Duration}
	\label{sec:checkout-duration}

	A supermarket models checkout time $T$ on $0\le T\le8$ minutes with the density $f(t)=t/32$ on that interval and $0$ elsewhere. Thus density follows one increasing straight line over the entire support.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:checkout-duration-c1}{1}
		{Characterise the checkout time $T$.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:checkout-duration-c1}

	The variable is \textbf{continuous} and has a \textbf{linear distribution}: it has bounded support and a valid PDF that changes linearly across the whole interval, without an interior mode.

	\secInicio[sec:continuous-probabilities][sec:checkout-duration]

	% ----------------------------------------------------------------
	\subsection{Short-Term Contract Value}
	\label{sec:contract-value}

	A broker models a contract value $V$, in thousands of dollars, on $0\le V\le10$ with density $f(v)=0.15-0.01v$ and zero outside. The density is nonnegative and follows one decreasing straight line over the full interval.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:contract-value-c1}{1}
		{Characterise the contract value $V$.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:contract-value-c1}

	The variable is \textbf{continuous} and has a \textbf{linear distribution}: on its bounded support the stated valid PDF decreases linearly from one endpoint to the other.

	\secInicio[sec:continuous-probabilities][sec:contract-value]

	% ----------------------------------------------------------------
	\subsection{Packed Coffee Weights}
	\label{sec:coffee-weights}

	An automated line fills many coffee bags. Their weights form an approximately symmetric bell-shaped pattern centred at $500$ grams; weights become increasingly uncommon as their distance below or above $500$ grams grows.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:coffee-weights-c1}{1}
		{Characterise the weight of a bag.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:coffee-weights-c1}

	The variable is \textbf{continuous} and has a \textbf{normal distribution}: measurements are approximately symmetric and bell-shaped around a central mean, with distant values rarer.

	\secInicio[sec:continuous-probabilities][sec:coffee-weights]

	% ----------------------------------------------------------------
	\subsection{Monthly Household Electricity Bills}
	\label{sec:electricity-bills}

	For a large group of similar apartments, monthly electricity bills cluster near \$75. Values equally far below and above \$75 occur with roughly equal frequency, and frequencies taper smoothly as distance from \$75 increases.

	\textbf{Questions/Tasks.}
	\begin{enumerate}
		\CriterionTask
		{subsec:electricity-bills-c1}{1}
		{Characterise a household's monthly electricity bill.}
	\end{enumerate}

	\subsubsection*{C1 - Characterise the random variable.}
	\phantomsection
	\label{subsec:electricity-bills-c1}

	The variable is \textbf{continuous} and has a \textbf{normal distribution}: the observations are approximately symmetric around a mean and progressively rarer away from it.

	\secInicio[sec:continuous-probabilities][sec:electricity-bills]


\end{document}
