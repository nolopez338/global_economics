\makeatletter
\def\input@path{{./}{../}{../../}{../../../}{../../../../}{preamble/}{../preamble/}{../../preamble/}{../../../preamble/}{../../../../preamble/}}
\makeatother

\input{preamble_G10_T1_practice_activities_c1_c3.tex}
\usetikzlibrary{calc,fit,shapes.geometric}

\definecolor{motblue}{HTML}{1F5A7A}
\definecolor{motgreen}{HTML}{3F7652}
\definecolor{motgold}{HTML}{B7791F}
\definecolor{motred}{HTML}{9B3E46}
\definecolor{motlight}{HTML}{F2F7F9}

\newtcolorbox{bigidea}[1][]{colback=motlight,colframe=motblue,
	boxrule=1pt,arc=2mm,title=\textbf{#1},fonttitle=\normalsize}
\tikzset{
	motnode/.style={draw=motblue,rounded corners=2mm,fill=motlight,
		align=center,inner sep=5pt,font=\small},
	motarrow/.style={-{Stealth[length=2.2mm]},thick,draw=motblue},
	criterion/.style={font=\scriptsize\bfseries,text=motred}
}

\title{Abstraction: A Super Tool for Decisions under Uncertainty\\
	\large One Mathematical Skeleton, Many Real-World Stories (C1--C4)}
\author{}
\date{}

\begin{document}
	\maketitle

	\begin{bigidea}[The journey in one line]
		\[
		\text{Real-world story}\longrightarrow\text{Abstract decision model}
		\longrightarrow\text{Payoff table}\longrightarrow\text{Profit/payoff calculation}
		\longrightarrow\text{Decision criterion}\longrightarrow\text{Interpretation in context}.
		\]
		These are not unrelated skills. Criteria C1--C4 are successive layers of one
		reusable workflow.
	\end{bigidea}

	\section{A surprising connection}

	On Monday, Maya helps a café decide whether to install a small or large
	production capacity before customer demand is known. Across town, Leo advises
	a farmer who must plant crop A or crop B before the season turns dry or rainy.
	Meanwhile, Sara compares two investment projects before the market proves weak
	or strong.

	At first, the problems seem unrelated: coffee cups, crops, and investments have
	different prices, risks, units, and consequences. Those are the story's
	\emph{clothing}. Look beneath it, however, and the same mathematical skeleton
	appears:
	\[
	\boxed{\text{choice before uncertainty}}
	\; + \;
	\boxed{\text{possible states}}
	\; + \;
	\boxed{\text{payoff for each combination}}.
	\]
	This pattern also appears when a company selects a production plan, a family
	evaluates a financial decision, a school enterprise plans output, or a
	government selects a policy. Recognising it is the super tool called
	\textbf{abstraction}.

	\section{From different stories to one notation}

	The nouns change, but their mathematical \emph{roles} stay the same. A
	decision-maker chooses an alternative \(A_i\), an uncertain state \(S_j\)
	occurs, and the combination determines the payoff \(P_{ay}(A_i,S_j)\).

	For example, Maya's ``large capacity'', Leo's ``crop B'', and Sara's
	``project B'' may all be labelled \(A_2\). They are not the same real-world
	choice; they simply occupy the same role in their respective decision models.

	The same abstraction works even when the number of alternatives or states
	changes:

	\begin{center}
		\resizebox{\textwidth}{!}{%
		\begin{tikzpicture}[
			node distance=8mm and 10mm,
			>=Latex,
			box/.style={
				draw=motblue,
				rounded corners=3pt,
				align=center,
				inner sep=5pt,
				text width=4.2cm
			},
			smallbox/.style={
				draw=motblue,
				rounded corners=3pt,
				align=center,
				inner sep=4pt,
				text width=3.9cm
			},
			genbox/.style={
				draw=motblue,
				fill=motlight,
				rounded corners=4pt,
				align=center,
				inner sep=6pt,
				text width=7.3cm
			}
			]

			% -------- Café context --------
			\node[box] (a1) at (-6.2,0)
			{
				\textbf{Café}\\
				small capacity, large capacity\\
				low demand, high demand
			};
			\node[smallbox, below=of a1] (a2)
			{
				\textbf{Same roles}\\
				alternatives: small, large\\
				states: low, high
			};
			\node[smallbox, below=of a2] (a3)
			{
				$A_1,A_2$\\[2pt]
				$S_1,S_2$\\[2pt]
				$P_{ay}(A_i,S_j)$
			};
			\draw[->] (a1) -- (a2);
			\draw[->] (a2) -- (a3);

			% -------- Farm context --------
			\node[box] (b1) at (0,0)
			{
				\textbf{Farm}\\
				crop A, crop B, crop C\\
				dry, normal, rainy
			};
			\node[smallbox, below=of b1] (b2)
			{
				\textbf{Same roles}\\
				alternatives: three crops\\
				states: three seasons
			};
			\node[smallbox, below=of b2] (b3)
			{
				$A_1,A_2,A_3$\\[2pt]
				$S_1,S_2,S_3$\\[2pt]
				$P_{ay}(A_i,S_j)$
			};
			\draw[->] (b1) -- (b2);
			\draw[->] (b2) -- (b3);

			% -------- Investment context --------
			\node[box] (c1) at (6.2,0)
			{
				\textbf{Investment}\\
				project A, project B, project C, $\ldots$\\
				weak, stable, strong, $\ldots$
			};
			\node[smallbox, below=of c1] (c2)
			{
				\textbf{Same roles}\\
				alternatives: many projects\\
				states: many market outcomes
			};
			\node[smallbox, below=of c2] (c3)
			{
				$A_1,A_2,A_3,\ldots$\\[2pt]
				$S_1,S_2,S_3,\ldots$\\[2pt]
				$P_{ay}(A_i,S_j)$
			};
			\draw[->] (c1) -- (c2);
			\draw[->] (c2) -- (c3);

			% -------- General form --------
			\node[genbox, below=2.8cm of b3] (gen)
			{
				\textbf{General decision notation}\\[4pt]
				$A_1,A_2,\ldots,A_m$\\[4pt]
				$S_1,S_2,\ldots,S_n$\\[4pt]
				$P_{ay}(A_i,S_j)$\\[3pt]
				{\small One notation for any number of alternatives and states}
			};

			\draw[->] (a3) -- (gen);
			\draw[->] (b3) -- (gen);
			\draw[->] (c3) -- (gen);

		\end{tikzpicture}%
		}
	\end{center}

	The notation preserves the common structure without erasing the original
	context: choose one alternative \(A_i\), observe one state \(S_j\), and obtain
	the payoff \(P_{ay}(A_i,S_j)\). The letters \(m\) and \(n\) allow the same notation
	to represent any number of alternatives and states.

	\section{The C1--C4 abstraction pipeline}

	\begin{center}
		\begin{tikzpicture}[node distance=4mm]
			\node[motnode,minimum width=8.2cm] (n1) {\textbf{1. Read the context}\\\textcolor{motred}{C1: identify}};
			\node[motnode,below=of n1,minimum width=8.2cm] (n2) {\textbf{2. Extract alternatives, event, states, consequences, and units}\\\textcolor{motred}{C1: identify}};
			\node[motnode,below=of n2,minimum width=8.2cm] (n3) {\textbf{3. Build the payoff-table skeleton}\\\textcolor{motred}{C2: organise}};
			\node[motnode,below=of n3,minimum width=8.2cm] (n4) {\textbf{4. Calculate or insert each payoff}\\\textcolor{motred}{C3: calculate}};
			\node[motnode,below=of n4,minimum width=8.2cm] (n5) {\textbf{5. Check completeness and units}\\\textcolor{motred}{C2--C3: validate}};
			\node[motnode,below=of n5,minimum width=8.2cm] (n6) {\textbf{6. Apply Maximax and/or Maximin}\\\textcolor{motred}{C4: compare}};
			\node[motnode,below=of n6,minimum width=8.2cm] (n7) {\textbf{7. Interpret the result and limitations in context}\\\textcolor{motred}{C4: interpret}};
			\foreach \x/\y in {n1/n2,n2/n3,n3/n4,n4/n5,n5/n6,n6/n7}
			\draw[motarrow] (\x)--(\y);
		\end{tikzpicture}
	\end{center}

	\section{C1: Identify the decision structure}

	Start by asking: Who makes the decision? What is the economic or financial
	context? What can that person or organisation choose? What uncertain event
	occurs \emph{after} the choice? Which states can it produce? What consequence
	belongs to each alternative--state pair? In what units will it be measured?

	Write the \(m\) alternatives and \(n\) states as
	\[
	A_1,A_2,\ldots,A_m
	\qquad\text{and}\qquad
	S_1,S_2,\ldots,S_n.
	\]
	The timeline prevents a common confusion:
	\[
	\text{Choose }A_i
	\quad\longrightarrow\quad
	\text{uncertainty is resolved as }S_j
	\quad\longrightarrow\quad
	\text{consequence and payoff }P_{ay}(A_i,S_j).
	\]

	\begin{itemize}
		\item The decision-maker controls the choice of \(A_i\).
		\item The decision-maker does not control which \(S_j\) occurs.
		\item The combination \((A_i,S_j)\) determines a consequence.
		\item The payoff assigns an economic or financial value to that consequence.
	\end{itemize}

	A \emph{consequence} may first be expressed in words: for example, ``the large
	café has unused equipment when demand is low.'' Its numerical \emph{payoff}
	might be a profit of \(-2\) thousand USD. The words explain what happens; the
	number makes economic comparison possible. Units are part of that meaning.

	\section{C2: Give the structure a table}

	The payoff table is the bridge between a verbal story and a mathematical
	comparison. Repository convention places \textbf{alternatives in rows},
	\textbf{states in columns}, and \(P_{ay}(A_i,S_j)\) in their corresponding cell:
	\[
	\mathbf P=
	\begin{pmatrix}
		P_{ay}(A_1,S_1)&P_{ay}(A_1,S_2)&\cdots&P_{ay}(A_1,S_n)\\
		P_{ay}(A_2,S_1)&P_{ay}(A_2,S_2)&\cdots&P_{ay}(A_2,S_n)\\
		\vdots&\vdots&\ddots&\vdots\\
		P_{ay}(A_m,S_1)&P_{ay}(A_m,S_2)&\cdots&P_{ay}(A_m,S_n)
	\end{pmatrix}.
	\]
	This is an \(m\times n\) table: \(m\) alternative rows and \(n\) state
	columns. Every pair \((A_i,S_j)\) must have exactly one cell---no gaps and no
	duplicates. Maya's, Leo's, and Sara's tables can therefore have the same shape
	even though every label and payoff has a different real-world meaning.

	\section{C3: Build the value in each cell}

	Sometimes a generic payoff is supplied directly and can simply be inserted. If
	it is built from generic payoff components, the reference convention is
	\[
	P_{ay}(A_i,S_j)
	=
	q(A_i,S_j)\bigl[v(A_i)+v(S_j)\bigr]+f(A_i)+f(S_j),
	\]
	where \(v(A_i)\) and \(v(S_j)\) are alternative-based and state-based
	variable payoff rates, while \(f(A_i)\) and \(f(S_j)\) are the corresponding
	fixed payoff components. This is a generic payoff, not necessarily profit.

	In a revenue-and-cost setting, \emph{revenue} is income earned, \emph{cost}
	is the value of resources used, and the payoff is specifically \emph{profit}:
	\[
	P(A_i,S_j)=R(A_i,S_j)-C(A_i,S_j).
	\]

	Imagine each payoff-table cell as an empty container. First assemble all its
	revenue pieces in \(R(A_i,S_j)\). Separately assemble all its cost pieces in
	\(C(A_i,S_j)\). Only then subtract the second container from the first.

	\begin{center}
		\begin{tikzpicture}[node distance=6mm]
			\node[motnode,fill=motgreen!12] (r) {revenue pieces\\variable + fixed};
			\node[motnode,right=of r,fill=motred!10] (c) {cost pieces\\variable + fixed};
			\node[motnode,right=of c,fill=motgold!12,minimum width=3.2cm] (p)
			{completed cell\\$P(A_i,S_j)=R-C$};
			\draw[motarrow] (r)--node[above,font=\small]{assemble \(R\)} (c);
			\draw[motarrow] (c)--node[above,font=\small]{subtract} (p);
		\end{tikzpicture}
	\end{center}

	Variable components depend on quantity; fixed components do not. A component
	labelled by \(A_i\) changes with the alternative, one labelled by \(S_j\)
	changes with the state, and \(q(A_i,S_j)\) may change in every cell. A general
	container broad enough for many simpler stories is
	\[
	R(A_i,S_j)=q(A_i,S_j)\bigl[r(A_i)+r(S_j)\bigr]
	+f_r(A_i)+f_r(S_j),
	\]
	\[
	C(A_i,S_j)=q(A_i,S_j)\bigl[c(A_i)+c(S_j)\bigr]
	+f_c(A_i)+f_c(S_j),
	\]
	and therefore \(P(A_i,S_j)=R(A_i,S_j)-C(A_i,S_j)\).

	Do not let the large formula hide the simple idea. Many stories use only some
	of its compartments:
	\begin{itemize}
		\item treat a missing component as zero;
		\item do not decompose a directly supplied total;
		\item never multiply a fixed amount by quantity;
		\item keep units compatible (for example, thousand USD with thousand USD);
		\item construct revenue and cost independently before subtracting.
	\end{itemize}
	For Maya, quantity might be cups sold; for Leo, tonnes harvested; for Sara,
	there may be no quantity at all because project profits are already supplied.
	The container adapts without changing the workflow.

	\section{C4: View one table through two decision lenses}

	C1--C3 construct a valid model and completed table. C4 asks how to compare its
	rows when no probabilities are supplied. The two criteria below answer
	different questions; neither is universally correct.

	\subsection{The optimistic lens: Maximax}
	Maximax selects the largest possible payoff. First define the row maximum for
	alternative \(A_i\) by
	\[
	M_i=\max_{j\in\{1,\ldots,n\}}P(A_i,S_j).
	\]
	Then select the alternative (or alternatives) with the largest row maximum:
	\[
	A_{\text{Maximax}}=
	\operatorname*{arg\,max}_{i\in\{1,\ldots,m\}}
	\left\{\max_{j\in\{1,\ldots,n\}}P(A_i,S_j)\right\}.
	\]

	\subsection{The protective lens: Maximin}
	Maximin selects the largest worst-case payoff. First define the row minimum for
	alternative \(A_i\) by
	\[
	m_i=\min_{j\in\{1,\ldots,n\}}P(A_i,S_j).
	\]
	Then select the alternative (or alternatives) with the largest row minimum:
	\[
	A_{\text{Maximin}}=
	\operatorname*{arg\,max}_{i\in\{1,\ldots,m\}}
	\left\{\min_{j\in\{1,\ldots,n\}}P(A_i,S_j)\right\}.
	\]

	One small table makes the two lenses visible. Suppose Maya's café profits are
	in \textbf{thousand USD}:
	\begin{center}
		\begin{tabular}{lrr}
			\toprule
			Alternative & Low demand \(S_1\) & High demand \(S_2\)\\
			\midrule
			Small capacity \(A_1\) & 6 & 14\\
			Large capacity \(A_2\) & -2 & 22\\
			\bottomrule
		\end{tabular}
	\end{center}

	\begin{center}
		\begin{tikzpicture}[node distance=8mm and 10mm]
			\node[motnode,minimum width=3.4cm,minimum height=1.2cm] (table)
			{one completed\\payoff table};
			\node[motnode,above right=5mm and 12mm of table,fill=motgold!12] (max)
			{optimistic lens\\row maxima: \((14,22)\)};
			\node[motnode,below right=5mm and 12mm of table,fill=motgreen!12] (min)
			{protective lens\\row minima: \((6,-2)\)};
			\node[motnode,right=of max] (mx) {Maximax\\selects \(A_2\)};
			\node[motnode,right=of min] (mn) {Maximin\\selects \(A_1\)};
			\draw[motarrow] (table)--(max); \draw[motarrow] (max)--(mx);
			\draw[motarrow] (table)--(min); \draw[motarrow] (min)--(mn);
		\end{tikzpicture}
	\end{center}

	Thus, the optimistic lens favours large capacity's upside, while the protective
	lens favours small capacity's stronger worst case. If two alternatives share
	the relevant row maximum under Maximax or row minimum under Maximin, report a
	\textbf{tie}; do not choose one arbitrarily.

	\section{Zoom back into the real world}

	A calculation is not yet a complete recommendation. Maximax focuses on upside
	and may overlook severe downside. Maximin protects against the worst payoff
	represented in the model but may reject attractive opportunities. Neither rule
	uses probabilities. Maya should therefore say more than ``\(A_2\)'': she
	should name ``large café capacity'', state the optimistic attitude, note the
	\(-2\) thousand USD downside, and explain the assumptions behind the payoffs.

	Leo and Sara must do the same zoom back in. What does the selected crop imply
	for the farm? What risk does the project expose the investor to? Are the states
	complete and the monetary estimates reasonable? A model is only as useful as
	its alternatives, states, payoffs, units, and assumptions. A criterion is a
	transparent decision lens, not an automatic declaration of the one universally
	correct choice. Other criteria exist, but they are outside our C1--C4 journey.

	\section{A reusable checklist}
	\begin{enumerate}[label=\textbf{\arabic*.},leftmargin=2em]
		\item What decision is being made?
		\item What are the alternatives \(A_i\)?
		\item What uncertain event occurs after the decision?
		\item What are the states \(S_j\)?
		\item What consequence belongs to each \((A_i,S_j)\)?
		\item What are the units?
		\item Are payoffs supplied, or must revenue and cost be calculated?
		\item Is every table cell complete?
		\item Are Maximax or Maximin appropriate for the stated attitude?
		\item What does the result mean in the original context?
		\item What risks, assumptions, ties, or limitations must be reported?
	\end{enumerate}

	\section{Final takeaway}
	\begin{bigidea}[Keep the skeleton; restore the meaning]
		\centering\large
		The context gives the decision meaning. Abstraction gives it structure. The
		payoff table gives it visibility. A decision criterion gives it a transparent
		rule for comparison. Interpretation brings the mathematics back to the real
		world.
	\end{bigidea}

	The stories will keep changing. Your super tool can travel with you.

\end{document}
